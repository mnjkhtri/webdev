# 🌐 Static Web Server - Nginx (Linux)

This setup uses **Nginx** to serve static files.

### 1️⃣ Automatic Setup
```sh
chmod +x setup.sh
./setup.sh
```

### 1️⃣ Stop Nginx
```sh
sudo systemctl stop nginx
```